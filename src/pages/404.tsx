import * as React from 'react';

// styles
const pageStyles = {
  color: '#232129',
  padding: '96px',
  fontFamily: 'Roboto, sans-serif, serif',
};

// markup
function NotFoundPage() {
  return <main style={pageStyles}>Не найдено :c</main>;
}

export default NotFoundPage;
